import { getProviderAndProgram } from "./helpers/env";

describe("emergencyPause / emergencyUnpause", () => {
  getProviderAndProgram();

  it("pause in emergency");
  it("unpause from emergency");
});
