import { getProviderAndProgram } from "./helpers/env";

describe("startRound", () => {
  getProviderAndProgram();

  it("happy path single asset (with oracle)");
  it("happy path group battle");
  it("fails before start time");
});
