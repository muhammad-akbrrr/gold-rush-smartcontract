import { getProviderAndProgram } from "./helpers/env";

describe("insertGroupAsset", () => {
  getProviderAndProgram();

  it("happy path");
  it("fails unauthorized");
  it("fails duplicate group symbol");
});
