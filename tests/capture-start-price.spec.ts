import { getProviderAndProgram } from "./helpers/env";

describe("captureStartPrice", () => {
  getProviderAndProgram();

  it("happy path with multiple assets (paired remaining accounts)");
  it("fails invalid remaining accounts length");
});
