import { getProviderAndProgram } from "./helpers/env";

describe("insertAsset", () => {
  getProviderAndProgram();

  it("happy path");
  it("fails max assets reached");
  it("fails invalid price feed account");
});
